#ifndef DEBUGGING_H
#define DEBUGGING_H

#ifdef DEBUGGING_H
#include <stdbool.h>

void debugging(bool debug_status, bool run_status, bool toggle_status, bool tab_status, int sprint_count, int tab_count);

#endif
#endif